<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CompactTheme" parent="@android:style/Theme.Material.Light" tools:targetApi="lollipop">
        <item name="android:colorPrimary">@color/colorCompactPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorCompactPrimaryDark</item>
        <item name="android:colorAccent">@color/colorCompactAccent</item>
        <item name="colorButtonNormal">@color/button_color</item>
    </style>

</resources>
